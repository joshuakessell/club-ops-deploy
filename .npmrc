# pnpm configuration
# Suppress peer dependency warnings for known compatible versions
# This file helps manage peer dependency warnings

# If you see peer dependency warnings that are safe to ignore,
# you can add them here using peer-dependency-rules in package.json instead

# Hoist ESLint to root so workspace packages can access it
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*typescript-eslint*


